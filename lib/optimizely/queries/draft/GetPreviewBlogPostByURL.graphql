query getPreviewBlogPostByURL(
  $locales: [Locales]
  $slug: String
  $version: String
) {
  BlogPost(
    locale: $locales
    where: {
      _metadata: { version: { eq: $version } }
      _and: { _metadata: { url: { default: { eq: $slug } } } }
    }
  ) {
    total
    item {
      title
      subheading
      author
      publishedDate
      body {
        html
        json
      }
      _modified
    }
  }
}

query getAllBlogPosts($locales: [Locales], $limit: Int) {
  BlogPost(locale: $locales, limit: $limit) {
    total
    items {
      title
      subheading
      author
      publishedDate
      isPinned
      _metadata {
        url {
          default
          hierarchical
        }
      }
    }
  }
}
